<div class="ui-generis-form">
    <h1>{{title}}</h1>
    <form action="{{form.action}}" method="{{form.method}}">
        <fieldset></fieldset>
        <div class="toolbar">
            <button type="submit">
                <span class="icon-save"/> {{submit.text}}
            </button>
        </div>
    </form>
</div>