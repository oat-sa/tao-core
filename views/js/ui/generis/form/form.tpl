<div class="ui-generis-form">
    <h2>{{title}}</h2>
    <form action="{{formAction}}" method="{{formMethod}}">
        <fieldset></fieldset>
        <div class="toolbar">
            <button type="submit">
                <span class="icon-save"/> {{submitText}}
            </button>
        </div>
    </form>
</div>