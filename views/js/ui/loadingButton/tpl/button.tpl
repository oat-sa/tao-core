<button class="loading-button btn-{{type}} small" type="button" title="{{title}}">
    <span class="spinner-icon icon-loop spinning"></span>
    <span class="start-icon icon-{{icon}}"></span>
    <span class="action-label">{{label}}</span>
    <span class="terminated-label">{{terminatedLabel}}</span>
</button>