
define('test/core/pluginLoader/mockDep', ['lodash'], function ( _ ){
    'use strict';

    return _.noop;
});

define('test/core/pluginLoader/mockAPlugin', ['test/core/pluginLoader/mockDep'], function( mockDep ){
    'use strict';

    return function(){
        return {
            name : 'mockA',
            init : mockDep
        };
    };
});

define('test/core/pluginLoader/mockBPlugin', ['test/core/pluginLoader/mockDep'], function( mockDep ){
    'use strict';

    return function(){
        return {
            name : 'mockB',
            init : mockDep
        };
    };
});
